{% extends 'base.html.twig' %}

{% block title %}Edit Lesson / {{ lesson.course.name}} / StudyOn{% endblock %}

{% block body %}
    <h1>Edit Lesson</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('course_show', {'id': lesson.course.id}) }}">back to list</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
